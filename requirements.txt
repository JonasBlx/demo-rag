# RAG demo dependencies — pinned to the exact versions validated in the image/CI.
# To upgrade intentionally: bump a pin (or rebuild), run
#   docker run --rm demo-rag pip freeze
# and update the versions below.

# --- Orchestration (split packages; the umbrella `langchain` isn't needed) ---
langchain-core==1.4.8
langchain-text-splitters==1.1.2

# --- LLM: Anthropic Claude (generation) ---
langchain-anthropic==1.4.7

# --- Embeddings: local by default (no API key) ---
# For the Voyage AI backend instead, add a pinned langchain-voyageai.
langchain-huggingface==1.2.2
sentence-transformers==5.6.0

# --- Vector store ---
langchain-chroma==1.1.0
chromadb==1.5.9

# --- Web UI ---
streamlit==1.58.0

# --- Doc ingestion ---
requests==2.34.2

# --- Config ---
python-dotenv==1.2.2
